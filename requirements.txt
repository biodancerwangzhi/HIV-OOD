# HIV-ESM-2 compute + plotting stack
# Python >= 3.10 recommended (developed on 3.12)
#
# Install (CPU-only; do not install CUDA/GPU wheels):
#   python -m venv venv
#   source venv/bin/activate   # Windows: venv\Scripts\activate
#   pip install -U pip
#   pip install -r requirements.txt
#
# Torch is pinned to the official CPU wheel index below.
# Do not replace with a CUDA torch build unless you intentionally want GPU.

# Prefer PyTorch CPU wheels; fall back to PyPI for everything else.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy==1.26.4
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.9.0
lightgbm==4.6.0
xgboost==3.3.0
matplotlib==3.11.0
seaborn==0.13.2
tqdm==4.68.3
joblib==1.5.3
fair-esm==2.0.0
torch==2.12.1+cpu
nbconvert==7.17.1
ipykernel==7.3.0
